# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Capacitor's `cap sync` recopies dist/ here on every build — same
# generated-output status as dist/ itself, not source.
ios/App/App/public

# Scratch/cleanup staging area, not project source.
_to_delete/
